import { useMemo, useState, type ReactNode } from 'react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { Link, Route, Switch, useLocation, useSearch } from 'wouter';
import {
  ArrowRight, ArrowUpRight, Bike, CalendarDays, Check, ChevronLeft, ChevronRight,
  Clock3, CircleAlert, CircleCheck, ClipboardList, Facebook, Instagram, LocateFixed,
  MapPin, Menu, MessageSquare, Phone, ShieldCheck, Sparkles, ToolCase,
  Wrench, X, Zap
} from 'lucide-react';
import {
  getListBookingsQueryKey, getListPartsQueryKey, getListServicesQueryKey,
  useCreateBooking, useCreateContactInquiry, useListBookings, useListParts, useListServices,
} from '@workspace/api-client-react';
import type { Booking, BookingInput, ContactInput, Part, Service } from '@workspace/api-client-react';
import { ErrorBoundary } from '@/components/error-boundary';
import { Toaster } from '@/components/ui/toaster';
import { TooltipProvider } from '@/components/ui/tooltip';
import NotFound from '@/pages/not-found';

const queryClient = new QueryClient();

function Logo() {
  return (
    <Link href="/" className="flex items-center gap-3" data-testid="link-logo">
      <span className="flex h-10 w-10 items-center justify-center rounded-xl bg-[hsl(var(--primary))] text-[hsl(var(--primary-foreground))] shadow-[4px_4px_0_hsl(var(--secondary))]">
        <Bike size={22} strokeWidth={2.6} />
      </span>
      <span className="leading-none">
        <span className="display-font block text-[22px] font-bold uppercase tracking-tight">Shriram</span>
        <span className="mono-font block pt-1 text-[9px] font-bold uppercase tracking-[.2em] text-[hsl(var(--muted-foreground))]">Automobiles</span>
      </span>
    </Link>
  );
}

function Nav() {
  const [open, setOpen] = useState(false);
  const [location] = useLocation();
  const links = [['/', 'Home'], ['/book-service', 'Book service'], ['/contact', 'Find us']];
  return (
    <header className="sticky top-0 z-40 border-b border-[hsl(var(--border))] bg-[hsl(var(--background)/.92)] backdrop-blur-md">
      <div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4 lg:px-8">
        <Logo />
        <nav className="hidden items-center gap-8 md:flex" aria-label="Main navigation">
          {links.map(([href, label]) => (
            <Link key={href} href={href} data-testid={`link-nav-${label.replace(' ', '-')}`} className={`mono-font text-[11px] font-bold uppercase tracking-[.14em] transition-colors hover:text-[hsl(var(--accent))] ${location === href ? 'text-[hsl(var(--accent))]' : 'text-[hsl(var(--muted-foreground))]'}`}>
              {label}
            </Link>
          ))}
        </nav>
        <div className="hidden items-center gap-3 md:flex">
          <a href="tel:+919876543210" data-testid="link-call-header" className="mono-font flex items-center gap-2 text-[11px] font-bold text-[hsl(var(--secondary))]"><Phone size={14} /> +91 98765 43210</a>
          <Link href="/book-service" data-testid="link-book-header" className="group flex items-center gap-2 rounded-lg bg-[hsl(var(--secondary))] px-4 py-2.5 text-sm font-bold text-[hsl(var(--background))] transition-transform hover:-translate-y-0.5">Book a visit <ArrowUpRight size={15} className="transition-transform group-hover:translate-x-0.5 group-hover:-translate-y-0.5" /></Link>
        </div>
        <button type="button" onClick={() => setOpen(!open)} aria-label="Toggle navigation" data-testid="button-toggle-nav" className="rounded-lg border border-[hsl(var(--border))] p-2 md:hidden">{open ? <X size={20} /> : <Menu size={20} />}</button>
      </div>
      {open && <nav className="border-t border-[hsl(var(--border))] px-5 py-4 md:hidden" aria-label="Mobile navigation">
        <div className="flex flex-col gap-1">
          {links.map(([href, label]) => <Link key={href} onClick={() => setOpen(false)} href={href} data-testid={`link-mobile-${label.replace(' ', '-')}`} className="rounded-lg px-3 py-3 text-sm font-bold hover:bg-[hsl(var(--muted))]">{label}</Link>)}
        </div>
      </nav>}
    </header>
  );
}

function Footer() {
  return <footer className="bg-[hsl(var(--secondary))] px-5 py-12 text-[hsl(var(--background))] lg:px-8">
    <div className="mx-auto grid max-w-7xl gap-10 md:grid-cols-[1.4fr_1fr_1fr]">
      <div><Logo /><p className="mt-6 max-w-xs text-sm leading-6 text-[hsl(var(--background)/.62)]">The neighborhood pit-stop for riders who expect the right part, the honest answer, and work that lasts.</p></div>
      <div><p className="mono-font mb-4 text-[10px] font-bold uppercase tracking-[.2em] text-[hsl(var(--primary))]">Navigate</p><div className="flex flex-col gap-3 text-sm text-[hsl(var(--background)/.72)]"><Link href="/" data-testid="link-footer-home">Home</Link><Link href="/book-service" data-testid="link-footer-book">Book service</Link><Link href="/contact" data-testid="link-footer-contact">Contact & location</Link></div></div>
      <div><p className="mono-font mb-4 text-[10px] font-bold uppercase tracking-[.2em] text-[hsl(var(--primary))]">Open today</p><p className="text-sm leading-6 text-[hsl(var(--background)/.72)]">Mon–Sat · 9:00 AM–7:30 PM<br />Sunday · 10:00 AM–2:00 PM</p><a href="tel:+919876543210" data-testid="link-footer-phone" className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-[hsl(var(--primary))]"><Phone size={14} /> +91 98765 43210</a></div>
    </div>
    <div className="mx-auto mt-12 flex max-w-7xl flex-col justify-between gap-4 border-t border-[hsl(var(--background)/.15)] pt-5 text-[10px] text-[hsl(var(--background)/.45)] md:flex-row"><span className="mono-font uppercase tracking-[.15em]">© 2024 Shriram Automobiles</span><span>Built for the ride home.</span></div>
  </footer>;
}

function Shell({ children }: { children: ReactNode }) {
  return <div className="grain min-h-[100dvh] bg-[hsl(var(--background))] text-[hsl(var(--foreground))]"><Nav />{children}<Footer /></div>;
}

function SectionKicker({ children }: { children: ReactNode }) {
  return <div className="mono-font mb-4 flex items-center gap-2 text-[10px] font-bold uppercase tracking-[.2em] text-[hsl(var(--accent))]"><span className="h-px w-7 bg-[hsl(var(--accent))]" />{children}</div>;
}

function IconFor({ icon, size = 23 }: { icon?: string; size?: number }) {
  const value = (icon || '').toLowerCase();
  if (value.includes('oil')) return <Zap size={size} />;
  if (value.includes('brake')) return <ShieldCheck size={size} />;
  if (value.includes('elect')) return <Sparkles size={size} />;
  if (value.includes('tyre') || value.includes('tire')) return <CircleCheck size={size} />;
  return <Wrench size={size} />;
}

function QueryState({ loading, error, empty, children, retry }: { loading: boolean; error: boolean; empty: boolean; children: ReactNode; retry?: () => void }) {
  if (loading) return <div className="grid gap-4 md:grid-cols-3">{[1, 2, 3].map((item) => <div key={item} className="h-48 animate-pulse rounded-2xl bg-[hsl(var(--muted))]" />)}</div>;
  if (error) return <div className="rounded-2xl border border-[hsl(var(--accent)/.35)] bg-[hsl(var(--accent)/.08)] p-8 text-center"><CircleAlert className="mx-auto mb-3 text-[hsl(var(--accent))]" /><p className="font-bold">Could not load this right now.</p><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">Please try again in a moment.</p>{retry && <button type="button" onClick={retry} data-testid="button-retry" className="mt-4 rounded-lg bg-[hsl(var(--secondary))] px-4 py-2 text-sm font-bold text-[hsl(var(--background))]">Retry</button>}</div>;
  if (empty) return <div className="rounded-2xl border border-dashed border-[hsl(var(--border))] p-10 text-center"><ToolCase className="mx-auto mb-3 text-[hsl(var(--muted-foreground))]" /><p className="font-bold">We are updating this list.</p><p className="mt-1 text-sm text-[hsl(var(--muted-foreground))]">Call us and we will help you find the right option.</p></div>;
  return <>{children}</>;
}

function ServiceCard({ service, onBook }: { service: Service; onBook: (id: string) => void }) {
  return <article className="group relative overflow-hidden rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-6 transition-all duration-300 hover:-translate-y-1 hover:border-[hsl(var(--primary))] hover:shadow-[7px_7px_0_hsl(var(--primary))]" data-testid={`card-service-${service.id}`}>
    <div className="mb-8 flex items-start justify-between"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-[hsl(var(--primary)/.2)] text-[hsl(var(--secondary))]"><IconFor icon={service.icon} /></div><span className="mono-font text-[10px] text-[hsl(var(--muted-foreground))]">{service.estimatedDuration}</span></div>
    <h3 className="display-font text-2xl font-bold uppercase">{service.name}</h3><p className="mt-2 min-h-12 text-sm leading-6 text-[hsl(var(--muted-foreground))]">{service.description}</p>
    <div className="mt-7 flex items-end justify-between border-t border-[hsl(var(--border))] pt-4"><div><span className="mono-font block text-[9px] uppercase tracking-wider text-[hsl(var(--muted-foreground))]">Starting at</span><span className="display-font text-xl font-bold">{service.startingPrice}</span></div><button type="button" onClick={() => onBook(service.id)} data-testid={`button-book-service-${service.id}`} className="group/btn flex items-center gap-2 text-sm font-bold text-[hsl(var(--accent))]">Book now <ArrowRight size={15} className="transition-transform group-hover/btn:translate-x-1" /></button></div>
  </article>;
}

function PartRow({ part }: { part: Part }) {
  return <article className="grid gap-3 border-b border-[hsl(var(--border))] py-5 first:pt-0 last:border-0 md:grid-cols-[1fr_130px_110px] md:items-center" data-testid={`row-part-${part.id}`}>
    <div><div className="flex items-center gap-2"><h3 className="font-bold">{part.name}</h3><span className="rounded-full bg-[hsl(var(--muted))] px-2 py-1 text-[9px] font-bold uppercase text-[hsl(var(--muted-foreground))]">{part.category}</span></div><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{part.brand} · {part.vehicleTypes.join(', ')}</p><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">{part.description}</p></div>
    <span className={`mono-font text-[10px] font-bold uppercase ${part.availability.toLowerCase().includes('stock') ? 'text-[hsl(var(--accent))]' : 'text-[hsl(var(--muted-foreground))]'}`}>{part.availability}</span>
    <a href="tel:+919876543210" data-testid={`link-enquire-part-${part.id}`} className="inline-flex items-center gap-2 text-sm font-bold text-[hsl(var(--secondary))] hover:text-[hsl(var(--accent))]">Enquire <ArrowUpRight size={14} /></a>
  </article>;
}

function Home() {
  const [, setLocation] = useLocation();
  const servicesQuery = useListServices({ query: { queryKey: getListServicesQueryKey() } });
  const partsQuery = useListParts(undefined, { query: { queryKey: getListPartsQueryKey() } });
  const services = servicesQuery.data ?? [];
  const parts = partsQuery.data ?? [];
  return <Shell>
    <main>
      <section className="page-grid relative overflow-hidden border-b border-[hsl(var(--border))]">
        <div className="mx-auto grid max-w-7xl gap-12 px-5 pb-20 pt-16 lg:grid-cols-[1.05fr_.95fr] lg:items-center lg:px-8 lg:pb-28 lg:pt-24">
          <div className="reveal"><SectionKicker>Est. 1998 · Your local pit-stop</SectionKicker><h1 className="display-font max-w-3xl text-6xl font-bold uppercase leading-[.88] tracking-tight sm:text-8xl">Keep your<br /><span className="text-[hsl(var(--accent))]">ride ready.</span></h1><p className="mt-7 max-w-lg text-lg leading-7 text-[hsl(var(--muted-foreground))]">Expert two-wheeler service, genuine parts, and straight answers. The kind of help that gets you back on the road without the runaround.</p><div className="mt-9 flex flex-wrap gap-3"><Link href="/book-service" data-testid="link-hero-book" className="group flex items-center gap-3 rounded-xl bg-[hsl(var(--accent))] px-5 py-3.5 text-sm font-bold text-[hsl(var(--accent-foreground))] transition-transform hover:-translate-y-1">Book a service <ArrowRight size={17} className="transition-transform group-hover:translate-x-1" /></Link><Link href="/contact" data-testid="link-hero-contact" className="flex items-center gap-3 rounded-xl border border-[hsl(var(--secondary))] px-5 py-3.5 text-sm font-bold text-[hsl(var(--secondary))] transition-colors hover:bg-[hsl(var(--secondary))] hover:text-[hsl(var(--background))]">Get directions <MapPin size={17} /></Link></div><div className="mt-12 flex items-center gap-8"><div><span className="display-font text-3xl font-bold">26</span><span className="ml-2 text-xs text-[hsl(var(--muted-foreground))]">years<br />on the road</span></div><div className="h-9 w-px bg-[hsl(var(--border))]" /><div><span className="display-font text-3xl font-bold">4.9</span><span className="ml-2 text-xs text-[hsl(var(--muted-foreground))]">rider<br />rating</span></div></div></div>
          <div className="reveal reveal-2 relative mx-auto w-full max-w-lg"><div className="absolute -right-2 -top-5 z-10 rounded-lg bg-[hsl(var(--secondary))] px-4 py-3 text-[hsl(var(--background))] shadow-[5px_5px_0_hsl(var(--accent))]"><span className="mono-font block text-[9px] uppercase tracking-[.15em] text-[hsl(var(--primary))]">Today at the shop</span><span className="mt-1 block text-sm font-bold">Walk-ins welcome</span></div><div className="relative aspect-[.9] overflow-hidden rounded-[2rem] bg-[hsl(var(--secondary))]"><div className="absolute inset-0 opacity-25" style={{ backgroundImage: 'radial-gradient(circle at 20% 20%, hsl(44 98% 55%) 1px, transparent 1px)', backgroundSize: '18px 18px' }} /><div className="absolute -bottom-12 -left-8 h-64 w-64 rounded-full border-[28px] border-[hsl(var(--primary))] opacity-90" /><div className="absolute bottom-10 right-10 flex h-56 w-56 items-center justify-center rounded-full border-[18px] border-[hsl(var(--accent))]"><div className="h-24 w-24 rounded-full border-[12px] border-[hsl(var(--background))]" /></div><div className="absolute left-8 top-1/2 -translate-y-1/2"><Wrench size={115} strokeWidth={1} className="text-[hsl(var(--background))]" /></div><span className="mono-font absolute bottom-7 left-7 text-[10px] uppercase tracking-[.2em] text-[hsl(var(--background)/.6)]">Parts / service / advice</span></div></div>
        </div>
        <div className="mono-font mx-auto flex max-w-7xl items-center gap-3 px-5 pb-6 text-[10px] font-bold uppercase tracking-[.16em] text-[hsl(var(--muted-foreground))] lg:px-8"><span className="pulse-line h-px w-8 bg-[hsl(var(--accent))]" />No drama. Just dependable work.</div>
      </section>
      <section className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="mb-10 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><SectionKicker>What we do</SectionKicker><h2 className="display-font max-w-xl text-5xl font-bold uppercase leading-[.9]">Small shop.<br />Serious standards.</h2></div><Link href="/book-service" data-testid="link-services-all" className="flex items-center gap-2 text-sm font-bold text-[hsl(var(--accent))]">See all service options <ArrowUpRight size={16} /></Link></div><QueryState loading={servicesQuery.isLoading} error={servicesQuery.isError} empty={!servicesQuery.isLoading && services.length === 0} retry={() => servicesQuery.refetch()}><div className="grid gap-4 md:grid-cols-3">{services.slice(0, 3).map((service) => <ServiceCard key={service.id} service={service} onBook={(id) => setLocation(`/book-service?service=${id}`)} />)}</div></QueryState></section>
      <section className="bg-[hsl(var(--secondary))] px-5 py-20 text-[hsl(var(--background))] lg:px-8 lg:py-24"><div className="mx-auto grid max-w-7xl gap-14 lg:grid-cols-[.75fr_1.25fr]"><div><SectionKicker>Genuine parts desk</SectionKicker><h2 className="display-font text-5xl font-bold uppercase leading-[.9]">The right<br /><span className="text-[hsl(var(--primary))]">part.</span><br />First time.</h2><p className="mt-6 max-w-sm text-sm leading-6 text-[hsl(var(--background)/.62)]">Tell us your bike, tell us what it is doing. We will help you find the fix, not just another box to carry home.</p><Link href="/contact" data-testid="link-parts-enquire" className="mt-7 inline-flex items-center gap-2 rounded-lg bg-[hsl(var(--primary))] px-4 py-3 text-sm font-bold text-[hsl(var(--secondary))]">Ask the parts desk <ArrowRight size={16} /></Link></div><div><div className="mb-6 flex items-center justify-between border-b border-[hsl(var(--background)/.15)] pb-4"><span className="mono-font text-[10px] uppercase tracking-[.18em] text-[hsl(var(--background)/.5)]">Live parts board</span><span className="flex items-center gap-2 text-[10px] text-[hsl(var(--primary))]"><span className="h-2 w-2 rounded-full bg-[hsl(var(--primary))]" />Updated regularly</span></div><QueryState loading={partsQuery.isLoading} error={partsQuery.isError} empty={!partsQuery.isLoading && parts.length === 0} retry={() => partsQuery.refetch()}><div>{parts.slice(0, 4).map((part) => <PartRow key={part.id} part={part} />)}</div></QueryState></div></div></section>
      <section className="mx-auto max-w-7xl px-5 py-20 lg:px-8 lg:py-28"><div className="grid gap-6 md:grid-cols-3"><div className="rounded-2xl bg-[hsl(var(--primary))] p-7 md:col-span-2"><span className="mono-font text-[10px] font-bold uppercase tracking-[.18em] text-[hsl(var(--secondary)/.7)]">The Shriram way</span><h2 className="display-font mt-8 max-w-xl text-4xl font-bold uppercase leading-[.95] text-[hsl(var(--secondary))]">A working bike is a small freedom. We protect it.</h2><div className="mt-10 grid gap-6 border-t border-[hsl(var(--secondary)/.15)] pt-6 sm:grid-cols-3"><div><ShieldCheck size={20} /><p className="mt-3 text-sm font-bold">Genuine first</p><p className="mt-1 text-xs text-[hsl(var(--secondary)/.65)]">Parts we would trust on our own bikes.</p></div><div><Clock3 size={20} /><p className="mt-3 text-sm font-bold">Clear timing</p><p className="mt-1 text-xs text-[hsl(var(--secondary)/.65)]">No vanishing acts. We call when it is ready.</p></div><div><MessageSquare size={20} /><p className="mt-3 text-sm font-bold">Plain speaking</p><p className="mt-1 text-xs text-[hsl(var(--secondary)/.65)]">You get the why, not just the bill.</p></div></div></div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--muted)/.55)] p-7"><LocateFixed size={22} className="text-[hsl(var(--accent))]" /><span className="mono-font mt-12 block text-[10px] font-bold uppercase tracking-[.18em] text-[hsl(var(--muted-foreground))]">Find us</span><h3 className="display-font mt-3 text-3xl font-bold uppercase">Near your<br />next turn.</h3><p className="mt-3 text-sm leading-6 text-[hsl(var(--muted-foreground))]">12, Station Road, Shriram Nagar<br />Open six days a week</p><Link href="/contact" data-testid="link-find-us-card" className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-[hsl(var(--secondary))]">View location <ArrowUpRight size={15} /></Link></div></div></section>
    </main>
  </Shell>;
}

const initialBooking: BookingInput = { fullName: '', phone: '', email: '', vehicleBrand: '', vehicleModel: '', vehicleType: 'Motorcycle', registrationNumber: '', vehicleAge: '', selectedServices: [], appointmentDate: '', timeSlot: '', problemDescription: '' };
const inputClass = 'mt-2 w-full rounded-lg border border-[hsl(var(--border))] bg-[hsl(var(--card))] px-3.5 py-3 text-sm outline-none transition-colors placeholder:text-[hsl(var(--muted-foreground))] focus:border-[hsl(var(--accent))] focus:ring-2 focus:ring-[hsl(var(--accent)/.18)]';

function Field({ label, name, value, onChange, placeholder, type = 'text', required = false }: { label: string; name: string; value: string; onChange: (value: string) => void; placeholder?: string; type?: string; required?: boolean }) {
  return <label className="block text-sm font-bold" htmlFor={name}>{label}{required && <span className="ml-1 text-[hsl(var(--accent))]">*</span>}<input id={name} name={name} type={type} value={value} onChange={(event) => onChange(event.target.value)} placeholder={placeholder} required={required} className={inputClass} data-testid={`input-${name}`} /></label>;
}

function BookingPage() {
  const search = useSearch();
  const [form, setForm] = useState<BookingInput>(() => {
    const serviceId = new URLSearchParams(search).get('service');
    return serviceId ? { ...initialBooking, selectedServices: [serviceId] } : initialBooking;
  });
  const [step, setStep] = useState(1);
  const [submitted, setSubmitted] = useState<Booking | null>(null);
  const [error, setError] = useState('');
  const servicesQuery = useListServices({ query: { queryKey: getListServicesQueryKey() } });
  const createBooking = useCreateBooking();
  const services = servicesQuery.data ?? [];
  const update = (key: keyof BookingInput, value: string | string[]) => setForm((previous) => ({ ...previous, [key]: value }));
  const selectedServiceNames = useMemo(() => services.filter((service) => form.selectedServices.includes(service.id)).map((service) => service.name), [services, form.selectedServices]);
  const canContinue = step === 1 ? form.selectedServices.length > 0 : step === 2 ? Boolean(form.fullName && form.phone && form.vehicleBrand && form.vehicleModel && form.registrationNumber) : Boolean(form.appointmentDate && form.timeSlot);
  const submit = () => { setError(''); createBooking.mutate({ data: form }, { onSuccess: (booking) => setSubmitted(booking), onError: () => setError('We could not save that booking. Please call us and we will arrange it directly.') }); };
  if (submitted) return <Shell><main className="mx-auto flex min-h-[70vh] max-w-3xl items-center px-5 py-16 lg:px-8"><div className="w-full rounded-3xl bg-[hsl(var(--secondary))] p-8 text-center text-[hsl(var(--background))] sm:p-14"><div className="mx-auto flex h-16 w-16 items-center justify-center rounded-full bg-[hsl(var(--primary))] text-[hsl(var(--secondary))]"><Check size={30} /></div><SectionKicker>Booking received</SectionKicker><h1 className="display-font text-5xl font-bold uppercase">You are on the list.</h1><p className="mx-auto mt-5 max-w-md text-sm leading-6 text-[hsl(var(--background)/.65)]">We will call to confirm the details shortly. Keep this reference handy.</p><div className="mono-font mx-auto mt-8 max-w-xs rounded-xl border border-[hsl(var(--background)/.15)] px-5 py-4 text-sm font-bold text-[hsl(var(--primary))]">{submitted.bookingId}</div><Link href="/" data-testid="link-booking-home" className="mt-8 inline-flex items-center gap-2 rounded-lg bg-[hsl(var(--primary))] px-5 py-3 text-sm font-bold text-[hsl(var(--secondary))]">Back to home <ArrowRight size={16} /></Link></div></main></Shell>;
  return <Shell><main className="page-grid mx-auto max-w-7xl px-5 py-14 lg:px-8 lg:py-20"><div className="mb-12 max-w-2xl"><SectionKicker>Book a service</SectionKicker><h1 className="display-font text-6xl font-bold uppercase leading-[.88]">Let’s get<br /><span className="text-[hsl(var(--accent))]">you sorted.</span></h1><p className="mt-5 text-base text-[hsl(var(--muted-foreground))]">Three quick steps. We will confirm your slot by phone.</p></div><div className="grid gap-10 lg:grid-cols-[1fr_330px]"><div><div className="mb-8 flex items-center gap-2">{['Service', 'Your bike', 'When'].map((label, index) => <div key={label} className="flex flex-1 items-center gap-2"><div className={`flex h-8 w-8 shrink-0 items-center justify-center rounded-full text-xs font-bold ${step >= index + 1 ? 'bg-[hsl(var(--accent))] text-[hsl(var(--accent-foreground))]' : 'bg-[hsl(var(--muted))] text-[hsl(var(--muted-foreground))]'}`}>{step > index + 1 ? <Check size={15} /> : index + 1}</div><span className={`hidden text-xs font-bold sm:block ${step === index + 1 ? '' : 'text-[hsl(var(--muted-foreground))]'}`}>{label}</span>{index < 2 && <div className="h-px flex-1 bg-[hsl(var(--border))]" />}</div>)}</div><div className="rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))] p-5 sm:p-8">
    {step === 1 && <div><h2 className="display-font text-3xl font-bold uppercase">What do you need?</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">Pick one or more. We will build the right visit around it.</p><QueryState loading={servicesQuery.isLoading} error={servicesQuery.isError} empty={!servicesQuery.isLoading && services.length === 0} retry={() => servicesQuery.refetch()}><div className="mt-7 grid gap-3 sm:grid-cols-2">{services.map((service) => { const selected = form.selectedServices.includes(service.id); return <button type="button" key={service.id} onClick={() => update('selectedServices', selected ? form.selectedServices.filter((id) => id !== service.id) : [...form.selectedServices, service.id])} data-testid={`button-select-service-${service.id}`} className={`flex items-center gap-4 rounded-xl border p-4 text-left transition-all ${selected ? 'border-[hsl(var(--accent))] bg-[hsl(var(--primary)/.16)] shadow-[3px_3px_0_hsl(var(--accent))]' : 'border-[hsl(var(--border))] hover:border-[hsl(var(--secondary))]'}`}><span className={`flex h-10 w-10 items-center justify-center rounded-lg ${selected ? 'bg-[hsl(var(--accent))] text-[hsl(var(--accent-foreground))]' : 'bg-[hsl(var(--muted))]'}`}><IconFor icon={service.icon} size={19} /></span><span><span className="block text-sm font-bold">{service.name}</span><span className="mt-1 block text-xs text-[hsl(var(--muted-foreground))]">{service.estimatedDuration} · {service.startingPrice}</span></span>{selected && <Check size={17} className="ml-auto text-[hsl(var(--accent))]" />}</button>; })}</div></QueryState></div>}
    {step === 2 && <div><h2 className="display-font text-3xl font-bold uppercase">Tell us about you & your bike</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">So we can have the right hands and parts ready.</p><div className="mt-7 grid gap-5 sm:grid-cols-2"><Field label="Full name" name="fullName" value={form.fullName} onChange={(v) => update('fullName', v)} placeholder="Your name" required /><Field label="Phone" name="phone" type="tel" value={form.phone} onChange={(v) => update('phone', v)} placeholder="+91 98765 43210" required /><Field label="Bike brand" name="vehicleBrand" value={form.vehicleBrand} onChange={(v) => update('vehicleBrand', v)} placeholder="Honda, TVS, Yamaha..." required /><Field label="Bike model" name="vehicleModel" value={form.vehicleModel} onChange={(v) => update('vehicleModel', v)} placeholder="Activa 6G, Splendor..." required /><Field label="Registration number" name="registrationNumber" value={form.registrationNumber} onChange={(v) => update('registrationNumber', v)} placeholder="MH 01 AB 1234" required /><Field label="Vehicle age" name="vehicleAge" value={form.vehicleAge} onChange={(v) => update('vehicleAge', v)} placeholder="Approx. age" /><Field label="Email" name="email" type="email" value={form.email} onChange={(v) => update('email', v)} placeholder="For booking updates" /><label className="block text-sm font-bold">Vehicle type<select name="vehicleType" value={form.vehicleType} onChange={(event) => update('vehicleType', event.target.value)} className={inputClass} data-testid="select-vehicleType"><option>Motorcycle</option><option>Scooter</option><option>Electric scooter</option><option>Other</option></select></label></div></div>}
    {step === 3 && <div><h2 className="display-font text-3xl font-bold uppercase">Pick a day that works</h2><p className="mt-2 text-sm text-[hsl(var(--muted-foreground))]">We will call to confirm your appointment before we start.</p><div className="mt-7 grid gap-5 sm:grid-cols-2"><Field label="Preferred date" name="appointmentDate" type="date" value={form.appointmentDate} onChange={(v) => update('appointmentDate', v)} required /><label className="block text-sm font-bold">Time slot<select name="timeSlot" value={form.timeSlot} onChange={(event) => update('timeSlot', event.target.value)} className={inputClass} data-testid="select-timeSlot"><option value="">Choose a time</option><option>9:00 AM – 11:00 AM</option><option>11:00 AM – 1:00 PM</option><option>2:00 PM – 4:00 PM</option><option>4:00 PM – 6:30 PM</option></select></label><label className="block text-sm font-bold sm:col-span-2">What is the bike doing? <textarea name="problemDescription" value={form.problemDescription} onChange={(event) => update('problemDescription', event.target.value)} placeholder="Optional — any sound, symptom, or recent change helps us prepare." className={`${inputClass} min-h-28 resize-y`} data-testid="textarea-problemDescription" /></label></div></div>}
    <div className="mt-8 flex flex-col-reverse justify-between gap-3 border-t border-[hsl(var(--border))] pt-6 sm:flex-row">{step > 1 ? <button type="button" onClick={() => setStep(step - 1)} data-testid="button-booking-back" className="flex items-center justify-center gap-2 rounded-lg border border-[hsl(var(--border))] px-4 py-3 text-sm font-bold"><ChevronLeft size={16} /> Back</button> : <span />}{step < 3 ? <button type="button" disabled={!canContinue} onClick={() => setStep(step + 1)} data-testid="button-booking-next" className="flex items-center justify-center gap-2 rounded-lg bg-[hsl(var(--secondary))] px-5 py-3 text-sm font-bold text-[hsl(var(--background))] disabled:cursor-not-allowed disabled:opacity-40">Continue <ChevronRight size={16} /></button> : <button type="button" disabled={!canContinue || createBooking.isPending} onClick={submit} data-testid="button-submit-booking" className="flex items-center justify-center gap-2 rounded-lg bg-[hsl(var(--accent))] px-5 py-3 text-sm font-bold text-[hsl(var(--accent-foreground))] disabled:cursor-not-allowed disabled:opacity-40">{createBooking.isPending ? 'Sending…' : 'Confirm request'} <Check size={16} /></button>}</div>{error && <p className="mt-4 flex items-center gap-2 text-sm text-[hsl(var(--destructive))]" data-testid="status-booking-error"><CircleAlert size={16} />{error}</p>}</div><aside className="h-fit rounded-2xl bg-[hsl(var(--secondary))] p-6 text-[hsl(var(--background))] lg:sticky lg:top-28"><span className="mono-font text-[10px] uppercase tracking-[.18em] text-[hsl(var(--primary))]">Your request</span><div className="mt-6 border-b border-[hsl(var(--background)/.15)] pb-5"><p className="text-xs text-[hsl(var(--background)/.55)]">Selected service</p>{selectedServiceNames.length ? <ul className="mt-3 space-y-2 text-sm font-bold">{selectedServiceNames.map((name) => <li key={name} className="flex items-center gap-2"><Check size={14} className="text-[hsl(var(--primary))]" />{name}</li>)}</ul> : <p className="mt-2 text-sm text-[hsl(var(--background)/.45)]">Choose a service to begin.</p>}</div><div className="mt-5 flex gap-3"><Phone size={18} className="shrink-0 text-[hsl(var(--primary))]" /><p className="text-xs leading-5 text-[hsl(var(--background)/.65)]">Need help choosing?<br /><a href="tel:+919876543210" data-testid="link-booking-call" className="font-bold text-[hsl(var(--primary))]">Call +91 98765 43210</a></p></div></aside></div></div></main></Shell>;
}

const initialContact: ContactInput = { name: '', phone: '', subject: '', message: '' };
function ContactPage() {
  const [form, setForm] = useState<ContactInput>(initialContact);
  const [sent, setSent] = useState(false);
  const [error, setError] = useState('');
  const inquiry = useCreateContactInquiry();
  const update = (key: keyof ContactInput, value: string) => setForm((previous) => ({ ...previous, [key]: value }));
  const submit = () => { setError(''); inquiry.mutate({ data: form }, { onSuccess: () => setSent(true), onError: () => setError('Message not sent. Please call us directly and we will help.') }); };
  return <Shell><main className="page-grid"><div className="mx-auto max-w-7xl px-5 py-14 lg:px-8 lg:py-20"><div className="grid gap-12 lg:grid-cols-[.8fr_1.2fr]"><div><SectionKicker>Contact & location</SectionKicker><h1 className="display-font text-6xl font-bold uppercase leading-[.88]">Come by.<br /><span className="text-[hsl(var(--accent))]">Call in.</span></h1><p className="mt-6 max-w-md text-base leading-7 text-[hsl(var(--muted-foreground))]">A question about a part, a noise you cannot place, or a bike that just will not start? Bring it over. We are good at getting to the useful bit.</p><div className="mt-10 space-y-6"><a href="tel:+919876543210" data-testid="link-contact-phone" className="flex items-start gap-4"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-[hsl(var(--primary))]"><Phone size={18} /></span><span><span className="mono-font block text-[10px] font-bold uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Call the desk</span><span className="mt-1 block font-bold">+91 98765 43210</span></span></a><div className="flex items-start gap-4"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-[hsl(var(--primary))]"><MapPin size={18} /></span><span><span className="mono-font block text-[10px] font-bold uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Workshop address</span><span className="mt-1 block font-bold">12, Station Road, Shriram Nagar</span><span className="mt-1 block text-sm text-[hsl(var(--muted-foreground))]">Near the old bus depot</span></span></div><div className="flex items-start gap-4"><span className="flex h-10 w-10 items-center justify-center rounded-lg bg-[hsl(var(--primary))]"><Clock3 size={18} /></span><span><span className="mono-font block text-[10px] font-bold uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Workshop hours</span><span className="mt-1 block font-bold">Mon–Sat · 9:00 AM–7:30 PM</span><span className="mt-1 block text-sm text-[hsl(var(--muted-foreground))]">Sunday · 10:00 AM–2:00 PM</span></span></div></div><div className="mt-12 flex gap-3"><a href="#" aria-label="Instagram" data-testid="link-instagram" className="rounded-lg border border-[hsl(var(--border))] p-2.5 hover:border-[hsl(var(--accent))]"><Instagram size={17} /></a><a href="#" aria-label="Facebook" data-testid="link-facebook" className="rounded-lg border border-[hsl(var(--border))] p-2.5 hover:border-[hsl(var(--accent))]"><Facebook size={17} /></a></div></div><div className="rounded-3xl bg-[hsl(var(--secondary))] p-6 text-[hsl(var(--background))] sm:p-9"><span className="mono-font text-[10px] font-bold uppercase tracking-[.18em] text-[hsl(var(--primary))]">Send a message</span>{sent ? <div className="flex min-h-[420px] flex-col items-center justify-center text-center"><CircleCheck size={48} className="text-[hsl(var(--primary))]" /><h2 className="display-font mt-5 text-4xl font-bold uppercase">Message received.</h2><p className="mt-3 max-w-sm text-sm leading-6 text-[hsl(var(--background)/.6)]">Thanks for reaching out. Someone from the shop will call you back soon.</p><button type="button" onClick={() => { setSent(false); setForm(initialContact); }} data-testid="button-new-contact" className="mt-7 rounded-lg bg-[hsl(var(--primary))] px-4 py-3 text-sm font-bold text-[hsl(var(--secondary))]">Send another message</button></div> : <div className="mt-7 space-y-5"><label className="block text-sm font-bold">Name<input value={form.name} onChange={(e) => update('name', e.target.value)} placeholder="Your name" className={`${inputClass} border-[hsl(var(--background)/.25)] bg-[hsl(var(--background)/.06)] text-[hsl(var(--background))]`} data-testid="input-contact-name" /></label><label className="block text-sm font-bold">Phone<input type="tel" value={form.phone} onChange={(e) => update('phone', e.target.value)} placeholder="+91 98765 43210" className={`${inputClass} border-[hsl(var(--background)/.25)] bg-[hsl(var(--background)/.06)] text-[hsl(var(--background))]`} data-testid="input-contact-phone" /></label><label className="block text-sm font-bold">What can we help with?<input value={form.subject} onChange={(e) => update('subject', e.target.value)} placeholder="Part enquiry, service question..." className={`${inputClass} border-[hsl(var(--background)/.25)] bg-[hsl(var(--background)/.06)] text-[hsl(var(--background))]`} data-testid="input-contact-subject" /></label><label className="block text-sm font-bold">Message<textarea value={form.message} onChange={(e) => update('message', e.target.value)} placeholder="Tell us what is going on." className={`${inputClass} min-h-32 resize-y border-[hsl(var(--background)/.25)] bg-[hsl(var(--background)/.06)] text-[hsl(var(--background))]`} data-testid="textarea-contact-message" /></label><button type="button" disabled={inquiry.isPending || !form.name || !form.phone || !form.subject || !form.message} onClick={submit} data-testid="button-submit-contact" className="flex w-full items-center justify-center gap-2 rounded-lg bg-[hsl(var(--primary))] px-4 py-3.5 text-sm font-bold text-[hsl(var(--secondary))] disabled:opacity-40">{inquiry.isPending ? 'Sending…' : 'Send message'} <ArrowRight size={16} /></button>{error && <p className="flex items-center gap-2 text-sm text-[hsl(var(--primary))]" data-testid="status-contact-error"><CircleAlert size={16} />{error}</p>}</div>}</div></div></div><div className="mx-auto max-w-7xl px-5 pb-20 lg:px-8"><div className="relative flex min-h-72 items-end overflow-hidden rounded-3xl bg-[hsl(var(--primary))] p-7 sm:min-h-80 sm:p-10"><div className="absolute right-10 top-8 h-48 w-48 rounded-full border-[22px] border-[hsl(var(--secondary)/.18)]" /><div className="absolute right-24 top-24 h-20 w-20 rounded-full bg-[hsl(var(--accent)/.65)]" /><div className="relative"><span className="mono-font text-[10px] font-bold uppercase tracking-[.18em] text-[hsl(var(--secondary)/.65)]">A landmark, not a pin</span><h2 className="display-font mt-3 text-4xl font-bold uppercase text-[hsl(var(--secondary))]">12 Station Road<br />Shriram Nagar</h2><a href="https://maps.google.com/?q=12+Station+Road+Shriram+Nagar" target="_blank" rel="noreferrer" data-testid="link-open-maps" className="mt-5 inline-flex items-center gap-2 rounded-lg bg-[hsl(var(--secondary))] px-4 py-3 text-sm font-bold text-[hsl(var(--background))]">Open in maps <ArrowUpRight size={16} /></a></div></div></div></main></Shell>;
}

function AdminPage() {
  const bookingsQuery = useListBookings({ query: { queryKey: getListBookingsQueryKey() } });
  const bookings = bookingsQuery.data ?? [];
  return <Shell><main className="mx-auto max-w-7xl px-5 py-14 lg:px-8 lg:py-20"><div className="flex flex-col justify-between gap-5 border-b border-[hsl(var(--border))] pb-8 md:flex-row md:items-end"><div><SectionKicker>Future-ready view</SectionKicker><h1 className="display-font text-6xl font-bold uppercase leading-[.88]">Booking<br /><span className="text-[hsl(var(--accent))]">desk.</span></h1></div><div className="rounded-xl bg-[hsl(var(--muted))] px-4 py-3"><span className="mono-font block text-[9px] uppercase tracking-[.15em] text-[hsl(var(--muted-foreground))]">Total requests</span><span className="display-font text-3xl font-bold">{bookings.length}</span></div></div><div className="mt-8 overflow-hidden rounded-2xl border border-[hsl(var(--border))] bg-[hsl(var(--card))]"><QueryState loading={bookingsQuery.isLoading} error={bookingsQuery.isError} empty={!bookingsQuery.isLoading && bookings.length === 0} retry={() => bookingsQuery.refetch()}><div className="hidden grid-cols-[1fr_1fr_150px_130px] gap-4 border-b border-[hsl(var(--border))] bg-[hsl(var(--muted)/.55)] px-5 py-3 md:grid"><span className="mono-font text-[10px] font-bold uppercase tracking-wider text-[hsl(var(--muted-foreground))]">Rider</span><span className="mono-font text-[10px] font-bold uppercase tracking-wider text-[hsl(var(--muted-foreground))]">Vehicle / service</span><span className="mono-font text-[10px] font-bold uppercase tracking-wider text-[hsl(var(--muted-foreground))]">Appointment</span><span className="mono-font text-[10px] font-bold uppercase tracking-wider text-[hsl(var(--muted-foreground))]">Status</span></div><div>{bookings.map((booking) => <BookingRow booking={booking} key={booking.id} />)}</div></QueryState></div></main></Shell>;
}
function BookingRow({ booking }: { booking: Booking }) {
  return <div className="grid gap-3 border-b border-[hsl(var(--border))] px-5 py-5 last:border-0 md:grid-cols-[1fr_1fr_150px_130px] md:items-center" data-testid={`row-booking-${booking.id}`}><div><p className="font-bold">{booking.fullName}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{booking.phone}</p></div><div><p className="font-bold">{booking.vehicleBrand} {booking.vehicleModel}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{booking.selectedServices.join(', ')}</p></div><div><p className="text-sm font-bold">{booking.appointmentDate}</p><p className="mt-1 text-xs text-[hsl(var(--muted-foreground))]">{booking.timeSlot}</p></div><span className="w-fit rounded-full bg-[hsl(var(--primary)/.25)] px-3 py-1 text-[10px] font-bold uppercase text-[hsl(var(--secondary))]">{booking.status}</span></div>;
}

function Router() {
  return <ErrorBoundary><Switch><Route path="/" component={Home} /><Route path="/book-service" component={BookingPage} /><Route path="/contact" component={ContactPage} /><Route path="/admin/bookings" component={AdminPage} /><Route component={NotFound} /></Switch></ErrorBoundary>;
}
function App() {
  return <QueryClientProvider client={queryClient}><TooltipProvider><Router /><Toaster /></TooltipProvider></QueryClientProvider>;
}
export default App;